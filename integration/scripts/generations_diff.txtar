# `gale generations` is list + rollback only.
# diff and remove are gone.

! exec gale generations diff
stderr 'accepts 0 arg'

! exec gale generations remove 1
stderr 'accepts 0 arg'

exec gale generations --help
stdout 'rollback'
! stdout 'diff'
! stdout 'remove'
