# `gale info <pkg>` prints recipe metadata for an
# installed package.

gale-fixture index $WORK/index
gale-fixture fetch-setup
exec gale install hello -g --index=$WORK/index

exec gale info hello
stdout 'hello'
stdout '1\.0'
