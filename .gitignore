# Binary output
/gale
.gale/

# Machine-specific
gale.lock

# Claude Code — .claude/ is tracked (agent bootstrap, hooks, settings).
# Only machine-local overrides stay out.
.claude/settings.local.json

# Build artifacts
*.tar.zst

# Superpowers specs
docs/superpowers/

# Pi artifacts
.pi/
.pi-lens/
gale.test
gale.toml.lock
gale.lock.lock
